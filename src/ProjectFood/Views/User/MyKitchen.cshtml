@model ProjectFood.Models.ViewModels.UserVM.MyKitchenVM

@{
    ViewData["Title"] = "MyKitchen";
}
<style>
    body {
        padding-top: 130px;
    }
</style>




<div class="ui-widget">
    <label for="myFood">Sök Matvara: </label>
    <input id="myFood">

</div>


<div class="ui-widget" style="margin-top:2em; font-family:Arial">
    MAT
    <table id="log" style="height: 200px; width: 300px;" class="ui-widget-content">
        @{int index = 0; }
        @{foreach (var item in Model.MyFood)
            {
                <tr id="@index">
                    <td id="1" onclick="doStuff(@item.FoodItem.Name, @index)">@item.FoodItem.Name</td>
                    <td id="@index" onclick="changeDate(@index)">@item.Expires</td>
                    <td onclick="removeItem(@index)">Ta bort</td>
                </tr>
                index++;
            }
        }
    </table>

    <input type="text" id="datepicker">
    <p id="selectedDateIndex"></p>
</div>

@*<div>
        <ul>
            @foreach (var item in Model.MyFood)
            {
                <li> GURGEL</li>
            }
        </ul>
    </div>*@

@*@{ foreach (var item in Model.AddableFood)
        {
            @item.Name <br />
        }
    }*@